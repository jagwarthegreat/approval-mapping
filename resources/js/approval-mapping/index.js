export function mountApprovalMapping() {
    // Optional Vue integration entrypoint for host apps.
    // Host app can import this module and mount Vue components.
    console.info('[approval-mapping] optional frontend assets loaded');
}
